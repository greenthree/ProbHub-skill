# 连续子段最大和

## 题目描述

给定 $T$ 组整数数组。对每组数组，求一个非空连续子段，使其中所有元素的和最大，并输出这个最大值。

## 输入格式

第一行输入一个整数 $T$（$1\le T\le 5$）。接下来每组数据占两行：第一行输入整数 $n$（$1\le n\le 20$），第二行输入 $n$ 个整数 $a_1,a_2,\ldots,a_n$（$-1000\le a_i\le 1000$）。

保证 $\sum_{i=1}^{T} n_i\le 64$。

## 输出格式

对于每组数据输出一行一个整数，表示该组数组的最大子段和。
