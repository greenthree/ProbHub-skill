{
  "name": "probhub",
  "version": "0.6.0",
  "description": "基于大语言模型 (LLM Agent) 和现代排版框架构建的 ACM/ICPC 自动化出题工作流。",
  "main": "bin/init.js",
  "bin": {
    "probhub-skill": "bin/init.js",
    "probhub": "bin/probhub.js"
  },
  "scripts": {
    "test": "python -m unittest discover -s tests -v",
    "check": "python -m compileall -q probhub scripts tests && python -m probhub --version && node --check bin/init.js && node --check bin/probhub.js && node --check bin/python.js && node --check scripts/webui/theme.js && node --check scripts/webui/tailwind-config.js && node --check scripts/webui/mathjax-config.js && node --check scripts/webui/app.js && npm --prefix compat/probhub-skill run check && npm run release:check && npm test",
    "release:check": "python scripts/check_release.py",
    "pack:check": "python scripts/check_release.py --pack",
    "prepublishOnly": "python scripts/check_release.py --pack --require-head-tag --require-clean"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/greenthree/ProbHub-skill.git"
  },
  "keywords": [
    "acm",
    "icpc",
    "problem-setting",
    "typst",
    "cyaron",
    "agent",
    "claude"
  ],
  "author": "greenthree",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/greenthree/ProbHub-skill/issues"
  },
  "homepage": "https://github.com/greenthree/ProbHub-skill#readme",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin/*.js",
    "CHANGELOG.md",
    "requirements.txt",
    "SKILL.md",
    "references/**",
    "scripts/*.py",
    "scripts/webui/**",
    "!scripts/check_clean_install.py",
    "!scripts/check_release.py",
    "probhub/**/*.py"
  ]
}
